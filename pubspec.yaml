name: fixer
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  collection: ^1.17.2
  parselyzer: ^0.1.0
  path: ^1.8.3

dev_dependencies:
  rexios_lints: ^5.0.0
  test: ^1.21.0
